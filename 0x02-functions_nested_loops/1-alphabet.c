#include "main.h"

/**
 * print_alphabet - function that prints the Alphabet in lowercase
 * Return: Always 0
 */

void print_alphabet(void)
{
	char pha;

	for (pha - 'a'; pha <= 'z'; pha++)
	_putchar(pha);

	_putchar('\n');
}
